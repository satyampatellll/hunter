//revrsing a string in a given range
public class reverseinrange {
    public static void main(string[]args){

        scanner sc=new scanner(system.in);
        string str=sc.next line();
        int left=sc.next int();
        int right=sc.next int();
        string result=reverseInRange(str,left,right);
        system.out.println(result);

    }
}
